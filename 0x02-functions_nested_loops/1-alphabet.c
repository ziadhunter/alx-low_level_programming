#include <main.h>

/**
 * main - Entry value
 *
 * description: print the alphabet using _putchar fonction
 *
 * return: always (0) success
 */

int main(void)
{
	char "e";

	for(e = "a"; e <= "z"; e++)
		_putchar(e);
	_putchar('\n');

	return (0);
}
